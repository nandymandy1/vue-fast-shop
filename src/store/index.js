import Auth from "./Auth";
import Cart from "./Cart";
import { createStore } from "vuex";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    Auth,
    Cart,
  },
});
